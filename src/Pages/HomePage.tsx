import React from 'react';
import Navbar from '../Components/Shared/Navbar';

const HomePage = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default HomePage;